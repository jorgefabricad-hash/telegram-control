services:
  - type: web
    name: telegram-control
    runtime: docker
    plan: free
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ALLOWED_USER_ID
        sync: false
      - key: DATABASE_URL
        sync: false
      - key: API_PORT
        value: 10000
    healthCheckPath: /
